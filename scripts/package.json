{
  "name": "@humpback/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/dev.ts",
    "typecheck": "tsc --project tsconfig.json --pretty false",
    "build": "pnpm run typecheck",
    "test": "node -e \"console.log('No script package tests are configured yet.')\"",
    "db:local:init": "tsx src/db-local-init.ts",
    "db:local:seed": "tsx src/db-local-seed.ts",
    "db:ingest": "tsx src/db-ingest.ts",
    "publish:static-viewer": "tsx src/publish-static-viewer.ts",
    "publish:timeline-data": "tsx src/publish-timeline-data.ts"
  },
  "dependencies": {
    "@aws-sdk/client-cloudfront": "3.1025.0",
    "@aws-sdk/client-dynamodb": "^3.1009.0",
    "@aws-sdk/client-s3": "3.1025.0",
    "@aws-sdk/lib-dynamodb": "^3.1009.0",
    "@humpback/api": "workspace:*"
  }
}
