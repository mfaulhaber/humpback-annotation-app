const mediaRoot = process.env.MEDIA_ROOT ?? "./local_media";

console.log("Local seed loading is not implemented yet.");
console.log(`Expected local media root: ${mediaRoot}`);
console.log(
  "Next step: load representative folders, samples, and labels for UI development.",
);
