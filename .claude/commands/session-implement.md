Read and execute the workflow defined in `docs/workflows/session-implement.md`,
including the browser UI verification guidance for `pnpm test:ui`,
`pnpm test:ui:visual`, and optional `pnpm test:ui:smoke` runs when relevant.
